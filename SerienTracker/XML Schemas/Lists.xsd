<?xml version="1.0" encoding="UTF-8"?>

<!-- Dieses Schema dient als Containerelement aller Listen-->

<xs:schema xmlns:xs="http://www.w3.org/2001/XMLSchema">

	<!-- Schema für einzelne Liste inkludieren -->
	<xs:include schemaLocation="List.xsd"/>

	<!-- Sammlung aller Listen -->
	<xs:element name="lists">
		<xs:complexType>
			<xs:sequence>
				<xs:element ref="list" maxOccurs="unbounded"/>
			</xs:sequence>
		</xs:complexType>
	</xs:element>

</xs:schema>
